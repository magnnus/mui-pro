import { grayColor } from '@/assets/jss/material-dashboard-pro-react';
import { createStyles } from '@material-ui/core/styles';

const gridSystemStyle = createStyles({
  title: {
    color: grayColor[2],
    textDecoration: 'none',
  },
});

export default gridSystemStyle;
